### 零、快速开始指南

**🚀 启动命令**
当你准备好题目后，请输入以下任一命令启动分析流程：
* `开始分析题目` 或 `启动八阶段分析` 或 `开始建模`

**📋 工作流程概览**
```mermaid
graph LR
    A[题目分析] --> B[模型选择]
    B --> C[数据处理]
    C --> D[模型建立]
    D --> E[模型求解]
    E --> F[结果分析]
    F --> G[模型检验]
    G --> H[论文素材输出]
```

**⚡ 关键约定**
* 每个阶段结束后，我会输出 ✅ **阶段完成标记** 和 📦 **输出清单**
* 你可以随时说 `暂停`、`跳过此步骤` 或 `重做上一步`
* 所有图表、公式、流程图将直接生成，无需你编写代码

---

### 一、前置信息与指令（请严格遵循）

**1.1 核心需求与输出格式**
* **模型适配性：** **根据题目特点选择最合适的模型**，而非盲目追求复杂度。传统数学模型（如线性规划、层次分析法、灰色预测）在某些场景下可能优于机器学习方法。**适合的才是最好的**。
* **模型创新性：** 在确保适配性的前提下，优先推荐组合模型或改进型模型（见下方模型库）。创新性应体现在"解决问题的有效性"，而非"算法的复杂性"。
* **代码要求：** **必须提供完整、可运行的代码**（Python/MATLAB/R），包括数据处理、模型训练、结果输出、可视化等所有环节。代码需添加详细注释，确保可复现。同时提供模型的**理论、结构和流程**的严谨性描述。
* **结果可视化：** 针对每个分析点（如预测对比、误差分布、参数敏感性），明确指定应采用的最佳图表类型（从下方图表库中选择），并提供**图表标题**、**图表说明文字**和**生成该图表的代码**。
* **论文写作素材：** 必须提供深入的**结果分析、结论和模型局限性评估**，作为我撰写论文的直接素材。所有输出必须符合学术论文规范。
* **可解释性要求：** 每个模型必须说明其"黑箱程度"和"可解释性水平"。在数学建模竞赛中，**可解释性往往比精度提升1-2%更重要**，评委更看重模型的合理性和创新思路。

**1.2 核心算法与模型库 (AI 知识库)**
在后续推荐模型时，请**优先**从以下列表中选择和组合，尤其是【创新型】列表中的模型：

| 类别         | 创新型算法名称（组合模型）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **预测类**   | ARIMA + LSTM, 随机森林 (RF) + 遗传算法 (GA), CNN + LSTM, SVM + 粒子群优化 (PSO), 灰色预测 (GM (1,1)) + BP 神经网络, GRU + 注意力机制 (Attention), XGBoost + 贝叶斯优化, 马尔可夫链 + 指数平滑法 (ETS), 小波分析 + LSTM, LightGBM + 堆叠集成 (Stacking), 模糊神经网络 (FNN) + GA, SARIMA + GARCH, 决策树 (DT) + AdaBoost, LSTM + SVR, 混沌时间序列 + PSO-BP                                                                                                                                                                             |
| **优化类**   | 遗传算法 (GA) + 模拟退火 (SA), 粒子群优化 (PSO) + 禁忌搜索 (TS), 蚁群算法 (ACO) + 贪婪算法 (Greedy), 支持向量机 (SVM) + 遗传算法 (GA), 模糊逻辑 (FL) + 粒子群优化 (PSO), 深度学习 (DNN) + 梯度下降 (GD), 多目标遗传算法 (MOGA) + 层次分析法 (AHP), 模拟退火 (SA) + 线性规划 (LP), 贝叶斯优化 (BO) + 随机森林 (RF), 禁忌搜索 (TS) + 0-1 整数规划 (IP), 人工蜂群算法 (ABC) + 差分进化 (DE), 模糊神经网络 (FNN) + 粒子群优化 (PSO), 多目标粒子群优化 (MOPSO) + 熵权法 (EWM), 遗传算法 (GA) + 约束规划 (CP), 模拟退火 (SA) + 神经网络 (NN) |
| **评价类**   | AHP + 熵权法, TOPSIS + 灰色关联分析, 模糊综合评价 + BP 神经网络, PCA + SVM, DEA + TOPSIS, 粗糙集 + 模糊聚类, 贝叶斯网络 + 证据理论, 随机森林 + AHP, 聚类分析 + 因子分析, 神经网络 + 遗传算法, SVM + 粒子群优化, 决策树 + 模糊逻辑, XGBoost + PCA                                                                                                                                                                                                                                                                                       |
| **基础算法** | **评价：** AHP, 熵权法, TOPSIS, 模糊综合评价, DEA。**预测：** Logistic, GM, ARMA, BP神经网络, 马尔可夫。**优化：** 线性规划, 整数规划, 动态规划, GA, SA, PSO。**分析/聚类：** PCA, T-SNE, K-means, 层次聚类, Pearson/Spearman 相关性。                                                                                                                                                                                                                                                                                                 |




**1.4 模型选择评分卡**
在推荐模型时，请使用以下评分卡（满分10分）进行量化评估：

| 评分维度     | 权重 | 评分标准                                                      |
| :----------- | :--- | :------------------------------------------------------------ |
| **适配性**   | 35%  | 模型假设与题目条件的匹配度（10=完全匹配，0=不适用）           |
| **可解释性** | 25%  | 结果的可解读性和理论支撑（10=白箱模型，0=纯黑箱）             |
| **创新性**   | 20%  | 相比传统方法的改进程度（10=重大创新，5=常规组合，3=传统方法） |
| **计算效率** | 10%  | 时间/空间复杂度（10=O(n)，5=O(n²)，0=O(2ⁿ)）                  |
| **鲁棒性**   | 10%  | 对噪声和参数变化的敏感度（10=极稳定，0=极敏感）               |

**综合得分 ≥ 7.0 分** 的模型方可推荐为主力方案。

**重要提示：**
- **适配性权重最高**：一个"简单但适配性高"的模型（如线性规划）可能比"复杂但不适配"的深度学习模型得分更高
- **可解释性权重第二**：数学建模竞赛中，评委希望看到清晰的建模思路，而非黑箱算法
- **创新性不是必须的**：如果传统方法已经足够好，不必强行创新
- **优先选择经典模型**：对于线性可分问题用线性规划、层次结构问题用AHP、时序趋势问题用ARIMA，不要"杀鸡用牛刀"

---

### **任务开始：**

请严格按照以下【八个阶段】的SOP（标准作业程序）逐步执行。
**在我提供题目（通常是 .md 文件或文本）后**，你将开始执行第一阶段。
每完成一个阶段，输出 ✅ **[阶段名称] 完成** 和 📦 **阶段输出清单**。

---

### 二、题目分析阶段

**🎯 阶段目标：** 全面解构题目，识别所有显性和隐性要求，建立问题框架。

**2.1 题目解析**
使用 Markdown 表格呈现以下内容：

| 序号 | 句子/段落原文 | 背景信息 | 核心问题 | 已知条件 | 约束条件 | 关键词提取 |
| :--- | :------------ | :------- | :------- | :------- | :------- | :--------- |
| 1    | (逐句填入)    | ✓/✗      | ✓/✗      | ✓/✗      | ✓/✗      | (专业术语) |
| 2    | ...           |          |          |          |          |            |

**2.2 小问划分与目标**
针对每个小问，填写下表：

| 小问编号 | 直接目标   | 隐含目标                 | 预期输出形式     | 难度评级 (1-5星) |
| :------- | :--------- | :----------------------- | :--------------- | :--------------- |
| 问题1    | (明确要求) | (如：为后续优化提供参数) | (表格/图表/数值) | ★★★☆☆            |
| 问题2    |            |                          |                  |                  |

**2.3 逻辑关系梳理**
使用 Mermaid 语法的 `graph TD` 或 `graph LR`，生成一个思维导图，展示各小问的关联性（前提条件）和条件依赖。

**2.4 题型分类与模型初选**
针对每个小问，填写下表：

| 小问  | 题型   | 判断依据             | 候选模型（从1.2中选择）        | 数据需求           |
| :---- | :----- | :------------------- | :----------------------------- | :----------------- |
| 问题1 | 预测类 | 题目要求预测未来趋势 | ARIMA+LSTM, XGBoost+贝叶斯优化 | 时序数据           |
| 问题2 | 优化类 | 题目要求最小化成本   | GA+SA, MOPSO+熵权法            | 约束条件、目标函数 |

---

**📦 阶段输出清单：**
- [ ] 题目解析表（逐句标注）
- [ ] 小问目标表（明确直接和隐含目标）
- [ ] 逻辑关系图（Mermaid 流程图）
- [ ] 题型分类表（初选模型）

✅ **题目分析阶段完成** → 进入 **模型选择阶段**

---

### 三、模型选择阶段

**🎯 阶段目标：** 为每个小问推荐2种模型方案（基础方案 + 创新方案），并使用评分卡量化评估。



针对每个小问，推荐 2 种模型方案（基础方案 + 创新方案），并从【1.2 核心算法与模型库】中优先选择。

**⚠️ 重要原则：**
- **传统模型优先**：如果题目特征明确（如线性关系、层次结构、确定性优化），优先使用传统数学模型
- **机器学习作为补充**：仅在数据量大、非线性强、难以建立解析模型时，才考虑机器学习方法
- **组合模型需谨慎**：组合模型并非总是更好，需确保每个子模型都有明确的作用，避免为了创新而创新

**3.1 模型推荐表**

| 维度             | 基础方案                               | 创新方案（推荐★）                                                    |
| :--------------- | :------------------------------------- | :------------------------------------------------------------------- |
| **模型名称**     | (如：线性规划 LP)                      | (如：多目标遗传算法 MOGA)                                            |
| **模型类型**     | 传统数学模型 / 机器学习模型 / 组合模型 | 传统数学模型 / 机器学习模型 / 组合模型                               |
| **核心原理**     | (1-2段通俗描述，避免过多公式)          | (1-2段通俗描述)                                                      |
| **适配性分析**   | (为何适合本题？结合题目约束/数据特点)  | (为何适合本题？)                                                     |
| **创新点**       | N/A                                    | (明确说明相比传统模型的优势，如果只是传统模型则填"无创新，但最适配") |
| **局限性**       | (清晰指出模型的固有弱点)               | (清晰指出模型的固有弱点)                                             |
| **数据要求**     | (样本量、特征数、数据类型)             | (样本量、特征数、数据类型)                                           |
| **计算复杂度**   | (时间/空间复杂度)                      | (时间/空间复杂度)                                                    |
| **可解释性**     | (高/中/低，需详细说明)                 | (高/中/低，需详细说明)                                               |
| **评分卡得分**   | (按1.4评分，满分10)                    | (按1.4评分，满分10)                                                  |
| **代码实现难度** | (简单/中等/困难)                       | (简单/中等/困难)                                                     |

**3.2 模型应用流程图（创新方案）**
使用 Mermaid 语法，展示创新方案的完整应用步骤。

**3.3 模型对比矩阵**
如果存在多个候选方案，使用雷达图或表格对比各方案的适配性、创新性、可解释性、计算效率、鲁棒性等维度，并给出综合得分和推荐理由。

---

**📦 阶段输出清单：**
- [ ] 每个小问的基础方案和创新方案
- [ ] 模型推荐表（包含8个维度分析）
- [ ] 创新方案的 Mermaid 流程图
- [ ] 模型对比矩阵（如适用）
- [ ] 最终推荐理由

✅ **模型选择阶段完成** → 进入 **数据处理阶段**

---

### 四、数据处理阶段

**🎯 阶段目标：** 识别数据问题，制定预处理方案，补充缺失数据，完成特征工程。


**4.1 题目数据清单**
列出题目提供的所有数据，并标注其特征：

| 数据编号 | 数据名称           | 数据类型 | 样本量 | 特征数 | 时间跨度  | 缺失率 | 数据来源 |
| :------- | :----------------- | :------- | :----- | :----- | :-------- | :----- | :------- |
| D1       | (如：历史销售数据) | 时序数据 | 120条  | 5个    | 2010-2023 | 3.2%   | 附件1    |
| D2       | (如：影响因素表)   | 截面数据 | 50条   | 8个    | 2023年    | 0%     | 附件2    |

**4.2 数据质量诊断**

**4.2.1 缺失值分析**
* **检测方法：** 缺失率统计、缺失模式可视化
* **可视化：** 使用 **热图**（横轴=特征，纵轴=样本，颜色=缺失/非缺失）
* **处理策略：**
  - 缺失率 < 5%：**线性插值法**（时序数据）或 **KNN填充**（截面数据）
  - 缺失率 5%-20%：**多重插补法 (MICE)** 或 **模型预测填充**
  - 缺失率 > 20%：**删除该特征** 或 **标记为特殊类别**
* **合理性依据：** (说明为何选择该策略)

**4.2.2 异常值分析**
* **检测方法：** IQR法（四分位距）、Z-Score法（$|Z| > 3$）、孤立森林
* **可视化：** 使用 **箱线图** 或 **小提琴图**，标注离群点
* **处理策略：**
  - **保留：** 如果异常值有实际意义（如疫情期间的突变）
  - **修正：** 使用 Winsorize 方法（将极值缩减至95th百分位）
  - **删除：** 如果确认为错误数据
* **合理性依据：** (结合实际业务场景说明)

**4.2.3 数据转换与标准化**
* **量纲不一致处理：**
  - **Z-Score 标准化：** $z = \frac{x - \mu}{\sigma}$ (适用于正态分布数据)
  - **Min-Max 归一化：** $x' = \frac{x - x_{min}}{x_{max} - x_{min}}$ (适用于有明确界限的数据)
  - **Log 变换：** $x' = \log(x)$ (适用于右偏分布数据)
* **选择依据：** (根据数据分布形态选择，需配合 **直方图+核密度图** 展示)

**4.2.4 高维数据降维**
* **降维方法选择：**
  - **PCA (主成分分析)：** 适用于线性关系为主的数据，保留累计贡献率 ≥ 85% 的主成分
  - **T-SNE：** 适用于非线性数据可视化（降至2-3维）
  - **因子分析：** 适用于潜在变量提取
* **可视化：** 使用 **碎石图**（特征值下降曲线）和 **累计贡献率曲线**
* **选择依据：** (说明为何选择 PCA 或 T-SNE)

**4.3 数据补充与假设**
如果题目数据不足，列出必须补充的数据项：

| 缺失数据项           | 获取途径       | 如无法获取的合理假设              | 假设依据               |
| :------------------- | :------------- | :-------------------------------- | :--------------------- |
| (如：2024年人口数据) | 国家统计局官网 | 假设按过去5年平均增长率 1.2% 外推 | 历史数据表明增长率稳定 |
| (如：运输成本系数)   | 行业报告       | 假设为销售额的 8%                 | 行业均值为 7%-9%       |

**4.4 特征工程**
* **新特征构造：** (如从日期提取"季度"、"是否节假日"；从销量计算"同比增长率")
* **特征选择：** 使用 **相关性热图** 或 **随机森林特征重要性** 筛选关键特征
* **可视化：** 使用 **相关性热图**（标注相关系数值）

---

**📦 阶段输出清单：**
- [ ] 题目数据清单表
- [ ] 缺失值分析报告 + 热图
- [ ] 异常值分析报告 + 箱线图/小提琴图
- [ ] 数据转换方案 + 分布图
- [ ] 降维分析 + 碎石图/累计贡献率曲线
- [ ] 数据补充表 + 假设依据
- [ ] 特征工程结果 + 相关性热图

✅ **数据处理阶段完成** → 进入 **模型建立阶段**

---

### 五、模型建立阶段

**🎯 阶段目标：** 将选定的创新模型转化为严谨的数学表达，明确变量、假设、公式和约束。


(针对每个小问选择的创新模型，详细说明建模步骤)

**5.1 变量定义表**

| 变量类型     | 符号         | 含义            | 单位 | 数据类型 | 取值范围             | 约束条件 |
| :----------- | :----------- | :-------------- | :--- | :------- | :------------------- | :------- |
| **输入变量** | $x_1$        | (如：历史销量)  | 万件 | 连续     | $[0, +\infty)$       | 非负     |
|              | $x_2$        | (如：广告投入)  | 万元 | 连续     | $[0, 500]$           | 预算约束 |
| **决策变量** | $y$          | (如：生产计划)  | 件   | 整数     | $[0, 10000]$         | 产能约束 |
| **目标变量** | $Z$          | (如：总成本)    | 元   | 连续     | 最小化               | N/A      |
| **中间变量** | $\epsilon_t$ | (如：ARIMA残差) | -    | 连续     | $(-\infty, +\infty)$ | N/A      |

**5.2 核心假设清单**

| 假设编号 | 假设内容               | 合理性论证                        | 简化的复杂度             | 对结果的影响                 |
| :------- | :--------------------- | :-------------------------------- | :----------------------- | :--------------------------- |
| H1       | (如：需求服从正态分布) | 历史数据 Shapiro-Wilk 检验 p>0.05 | 避免复杂的非参数分布估计 | 影响预测区间，误差约 ±5%     |
| H2       | (如：成本函数为线性)   | 单位成本在产量范围内保持恒定      | 忽略规模经济效应         | 可能低估大规模生产的成本优势 |
| H3       | (如：无外部冲击)       | 假设政策环境稳定                  | 不考虑突发事件（如疫情） | 极端情况下模型失效           |

**5.3 核心公式推导**

**5.3.1 目标函数构建**
* **公式 1（总成本函数）：**
$$
Z = \sum_{i=1}^{n} (c_i \cdot x_i + f_i \cdot y_i) + C_{\text{fixed}}
$$
* **物理意义：** 总成本由变动成本（$c_i \cdot x_i$）、固定成本（$C_{\text{fixed}}$）和决策相关成本（$f_i \cdot y_i$）三部分组成。
* **推导过程：** (如适用，说明如何从企业成本结构推导出该公式)

**5.3.2 约束条件**
* **公式 2（产能约束）：**
$$
\sum_{i=1}^{n} y_i \leq Y_{\max}
$$
* **物理意义：** 所有产品的总生产量不能超过工厂的最大产能。

* **公式 3（需求满足约束）：**
$$
y_i \geq D_i, \quad \forall i
$$
* **物理意义：** 每种产品的生产量必须至少满足预测需求。

**5.3.3 模型迭代公式（如适用于优化算法）**
* **公式 4（遗传算法交叉操作）：**
$$
x_{\text{new}} = \alpha \cdot x_1 + (1 - \alpha) \cdot x_2, \quad \alpha \in [0, 1]
$$
* **物理意义：** 通过父代个体的线性组合生成子代，保留优良基因。

* **公式 5（粒子群速度更新）：**
$$
v_{i}^{t+1} = \omega \cdot v_{i}^{t} + c_1 \cdot r_1 \cdot (p_{i}^{\text{best}} - x_{i}^{t}) + c_2 \cdot r_2 \cdot (g^{\text{best}} - x_{i}^{t})
$$
* **物理意义：** 粒子的速度由惯性项、个体最优引导项和全局最优引导项三部分决定，平衡探索与开发。

**5.4 建模流程图（Mermaid 语法）**

```mermaid
graph TD
    A[定义变量: x, y, Z] --> B[设定假设: H1, H2, H3]
    B --> C[构建目标函数: Z = f(x, y)]
    C --> D[添加约束条件: g(x, y) ≤ 0]
    D --> E{模型类型判断}
    E -->|预测类| F[划分训练集/测试集]
    E -->|优化类| G[初始化种群/粒子]
    F --> H[模型训练: ARIMA+LSTM]
    G --> I[迭代优化: GA+SA]
    H --> J[模型验证]
    I --> J
    J --> K[输出结果]
```

**5.5 模型可行性检验**
* **理论可行性：** 目标函数是否有界？约束条件是否相容？
* **计算可行性：** 估算求解时间（基于样本量和算法复杂度）
* **实际可行性：** 模型假设是否过于理想化？是否考虑了关键约束？

---

**📦 阶段输出清单：**
- [ ] 变量定义表（7列完整信息）
- [ ] 核心假设清单（5列详细论证）
- [ ] 核心公式推导（每个公式标注物理意义）
- [ ] 建模流程图（Mermaid 格式）
- [ ] 模型可行性检验报告

✅ **模型建立阶段完成** → 进入 **模型求解阶段**

---

### 六、模型求解阶段

**🎯 阶段目标：** 明确求解步骤、参数设置、输出要求，确保结果可复现。



(针对上述模型，详细说明求解步骤和输出内容，**必须提供完整可运行的代码**)

**6.1 求解步骤说明**

**步骤 1：数据输入与预处理（需提供代码）**
* **操作：** 导入清洗后的数据，进行标准化/归一化处理
* **关键参数：** 无
* **注意事项：** 确保训练集与测试集划分比例（通常 7:3 或 8:2）
* **代码要求：** 包括数据读取、缺失值处理、异常值检测、标准化等完整代码

**步骤 2：模型参数初始化（需提供代码）**
* **操作：** 设定模型的超参数
* **关键参数（示例）：**
  - **遗传算法 (GA)：** 种群规模 $N = 100$，交叉概率 $P_c = 0.8$，变异概率 $P_m = 0.01$，最大迭代次数 $G_{\max} = 500$
  - **模拟退火 (SA)：** 初始温度 $T_0 = 1000$，冷却速率 $\alpha \in [0.90, 0.99]$（需调试），终止温度 $T_{\text{end}} = 0.01$
  - **LSTM：** 隐藏层单元数 $h = 64$，学习率 $\eta = 0.001$，批次大小 $B = 32$，训练轮数 $E = 100$
  - **线性规划：** 求解器选择（如 scipy.optimize.linprog, PuLP, Gurobi）
  - **AHP：** 一致性比率阈值 CR < 0.1
* **注意事项：** 参数应通过**网格搜索**或**贝叶斯优化**进行调优
* **代码要求：** 完整的参数设置和模型初始化代码

**步骤 3：模型训练/求解（需提供代码）**
* **操作：** 执行模型算法
  - **预测类：** 拟合训练集，监控验证集损失函数
  - **优化类：** 迭代更新种群/粒子，记录每代最优解
  - **评价类：** 计算权重，进行一致性检验
* **关键参数：** 收敛判据（如连续 20 代无改善则停止）
* **注意事项：** 记录中间过程（如迭代曲线、损失函数变化）
* **代码要求：** 
  - 传统模型：完整的求解代码（如线性规划的约束构建、目标函数定义）
  - 机器学习模型：完整的训练循环、损失计算、模型保存

**步骤 4：结果输出与验证（需提供代码）**
* **操作：** 输出最优解/预测值，计算评价指标
* **评价指标（按题型选择）：**
  - **预测类：** RMSE（均方根误差）、MAPE（平均绝对百分比误差）、R²（决定系数）
  - **优化类：** 最优目标函数值、约束满足情况、求解时间
  - **评价类：** 一致性检验（CR < 0.1）、权重分布、排序结果
* **注意事项：** 对比基线模型，证明创新模型的优越性
* **代码要求：** 
  - 结果保存（CSV/Excel/JSON格式）
  - 评价指标计算函数
  - 结果可视化代码（见6.2）

**步骤 5：代码整合与注释**

**⚠️ 重要：代码文件命名规范（必须严格遵守）**

针对每个小问，必须生成以下两个独立的 Python 文件：

1. **问题X.py** - 核心求解代码
   - 包含：数据处理、模型定义、模型训练/求解、结果输出
   - 功能：完成该问题的完整求解流程，输出数值结果和中间数据
   - 输出：将关键结果保存为 CSV/Excel 文件（如 `问题X_结果.csv`）

2. **问题X_可视化.py** - 可视化代码
   - 包含：读取 `问题X.py` 的输出结果，生成所有图表
   - 功能：严格按照 **6.2 可视化输出要求** 生成所有图表
   - 输出：保存所有图表为高分辨率图片文件（如 `图X-1_描述.png`）
   - **必须包含：** 图表编号、标题、说明文字、数据来源、关键发现

**文件命名示例：**
```
问题1.py                    # 问题1的核心求解代码
问题1_可视化.py             # 问题1的所有图表生成代码
问题1_结果.csv              # 问题1的数值结果（由问题1.py生成）
图1-1_预测结果对比.png      # 问题1的第1张图（由问题1_可视化.py生成）
图1-2_误差分布分析.png      # 问题1的第2张图
...

问题2.py                    # 问题2的核心求解代码
问题2_可视化.py             # 问题2的所有图表生成代码
问题2_结果.csv              # 问题2的数值结果
图2-1_优化迭代曲线.png      # 问题2的第1张图
图2-2_路径可视化.png        # 问题2的第2张图
...
```



**6.2 可视化输出要求（必须提供绘图代码）**

**⚠️ 重要说明：** 以下图表规范是论文撰写的基础。所有图表必须在问题求解阶段完成，输出高质量图片文件，并提供图表说明文字。论文撰写时直接引用这些图表，无需重新制作。

**⚠️ 代码组织要求：** 所有可视化代码必须独立保存在 `问题X_可视化.py` 文件中，不要与求解代码混在一起。每个图表对应一个独立的函数，函数名为 `plot_figure_X_Y()`，其中 X 是问题编号，Y 是图序号。

明确列出求解成功后需要输出的**图表清单**，以及每张图表需要传达的**核心信息**。**每张图表都必须提供完整的绘图代码（Python matplotlib/seaborn 或 MATLAB）。**

#### 6.2.1 图表输出清单

| 图表编号 | 图表名称       | 图表类型                  | 核心信息                                         | 坐标轴/图例                            | 代码库                     |
| :------- | :------------- | :------------------------ | :----------------------------------------------- | :------------------------------------- | :------------------------- |
| 图1      | 预测结果对比   | 时间序列图 / 多系列折线图 | 展示「实际值」与「预测值」的拟合度，标注 RMSE 值 | X轴=时间，Y轴=数值，图例=实际值/预测值 | matplotlib.pyplot          |
| 图2      | 误差分布分析   | 直方图+核密度图           | 检验误差是否符合正态分布（均值≈0）               | X轴=误差值，Y轴=频数/密度              | seaborn.histplot + kdeplot |
| 图3      | 优化迭代曲线   | 基础折线图                | 展示目标函数随迭代次数的收敛过程                 | X轴=迭代次数，Y轴=目标函数值           | matplotlib.pyplot.plot     |
| 图4      | 优化路径可视化 | 基础散点图 + 基础折线图   | 展示节点（散点）和路径（折线），标注起点/终点    | X轴/Y轴=空间坐标                       | plt.scatter + plt.plot     |
| 图5      | 评价权重分布   | 基础柱状图 / 环形图       | 展示各指标的最终权重分配，标注权重值             | X轴=指标名称，Y轴=权重值               | plt.bar / plt.pie          |
| 图6      | 相关性热图     | 相关性热图                | 展示特征之间的相关系数矩阵，标注系数值           | 横纵轴=特征名称，色标=相关系数         | seaborn.heatmap            |
| 图7      | 敏感性分析     | 多系列折线图 / 雷达图     | 展示关键参数变化对结果的影响                     | X轴=参数变化幅度，Y轴=目标变化         | plt.plot / plt.polar       |

#### 6.2.2 图表编号与命名规范

**编号规则：**
- **格式：** 图 X-Y：[描述性标题]（X=章节号，Y=图序号）
- **示例：** 图 3-1：2010-2023年GDP实际值与预测值对比
- **注意：** 章节号必须与论文正文对应，图序号按出现顺序递增，不可跳号

**描述性标题的 5 要素：**
1. **时间范围：** 明确数据的时间跨度（如 2010-2023年）
2. **变量名称：** 说明展示的是哪些指标（如 GDP、销量、成本）
3. **对比维度：** 说明对比了哪些模型/方法（如 实际值与预测值、方案A与方案B）
4. **单位信息：** 明确数据单位（如 万元、%、件）
5. **图表类型：** 如有必要，说明是散点图、折线图、热图等

**标题位置：**
- **图片标题在下方**（图片下方居中）
- **表格标题在上方**（表格上方居中）

#### 6.2.3 图表说明文字规范（Caption）

**每张图表必须包含的 3 部分说明文字：**

1. **数据来源（Data Source）：**
   - 标注数据来自哪里、何时获取
   - **示例：** "数据来源：国家统计局2010-2023年季度数据"
   - **示例：** "数据来源：附件1原始数据，经过缺失值填充和异常值处理"

2. **关键发现（Key Findings）：**
   - 用 1-2 句话说明图表的核心信息


2. **关键发现（Key Findings）：**
   - 用 1-2 句话说明图表的核心信息

3. **异常说明（Anomaly Explanation）：**
   - 如有异常值或特殊情况，必须解释原因

#### 6.2.4 不同图表类型的标注要求

**折线图（Line Chart）：**
- **横轴：** 必须标注变量名+单位
- **纵轴：** 必须标注变量名+单位
- **图例：** 清晰说明每条线代表什么
- **关键点标注：** 用文字或箭头标注重要拐点

**散点图（Scatter Plot）：**
- **回归线：** 如做了回归分析，必须画出拟合线并标注回归方程和 R²
- **置信区间：** 如有，用阴影区域表示 95% 置信区间
- **异常点标注：** 用不同颜色或形状标注离群点，并在说明文字中解释

**柱状图（Bar Chart）：**
- **数值标注：** 每个柱子顶部标注具体数值
- **基准线：** 如有比较标准，画虚线表示

**热图（Heatmap）：**
- **色标（Colorbar）：** 右侧必须有色标说明数值范围
- **数值标注：** 在每个格子内标注具体数值（相关系数保留2位小数）
- **对角线说明：** 相关性矩阵的对角线都是 1，可用特殊颜色标注

**箱线图（Box Plot）：**
- **异常值标注：** 用圆圈或星号标注超出 1.5 倍 IQR 的异常点
- **中位数线：** 箱体中间的线表示中位数，必须在说明文字中指出
- **四分位数说明：** 在图表说明中解释箱体上下边界含义

#### 6.2.5 图表美观性要求

**配色方案（重要）：**

**基础配色原则：**
- **底色：** 纯白色背景（`#FFFFFF`），避免灰色或彩色背景
- **色系：** 浅色系为主，避免深色系（深蓝、深红、深绿等）
- **饱和度：** 控制在 40%-60%，避免过于鲜艳或灰暗
- **亮度：** 控制在 70%-90%，确保柔和舒适
- **风格统一：** 全文所有图表使用统一配色方案，切忌花哨
- **评委印象：** 配色直接影响论文的专业度和评委的第一印象

**推荐配色方案（按优先级）：**

1. **官方配色方案（最推荐）：**
   - **Matplotlib 默认配色（tab10）：** 浅蓝、浅橙、浅绿、浅红、浅紫、浅棕、浅粉、浅灰、浅黄绿、浅青
   - **Seaborn 默认配色（deep）：** 柔和的蓝、橙、绿、红、紫、棕
   - **ColorBrewer 配色（Set2）：** 学术论文常用，色盲友好
   ```python
   # 推荐使用方式
   import matplotlib.pyplot as plt
   import seaborn as sns
   
   # 方案1：Matplotlib 官方配色
   plt.style.use('default')  # 使用默认配色
   
   # 方案2：Seaborn 柔和配色
   sns.set_palette("deep")  # 或 "muted", "pastel"
   
   # 方案3：ColorBrewer 学术配色
   sns.set_palette("Set2")  # 或 "Set1", "Paired"
   ```

2. **对比色搭配（用于对比实验）：**
   - **实际值 vs 预测值：** 浅蓝（`#6BAED6`）vs 浅橙（`#FD8D3C`）
   - **方案A vs 方案B：** 浅绿（`#74C476`）vs 浅紫（`#9E9AC8`）
   - **基线方法：** 浅灰（`#BDBDBD`）
   - **最优方案：** 浅红（`#FC8D62`）突出显示

3. **渐变色搭配（用于热图）：**
   - **正向渐变：** 白色 → 浅蓝 → 中蓝（`#FFFFFF` → `#DEEBF7` → `#6BAED6`）
   - **双向渐变：** 浅蓝 → 白色 → 浅红（`#6BAED6` → `#FFFFFF` → `#FC8D62`）
   - **避免使用：** 深色渐变（如深蓝→深红）、彩虹色（Jet配色）
   ```python
   # 推荐热图配色
   sns.heatmap(data, cmap='Blues', vmin=0, vmax=1)  # 单色渐变
   sns.heatmap(data, cmap='RdBu_r', center=0)       # 双色渐变（以0为中心）
   ```

4. **色盲友好配色（必须考虑）：**
   - **避免：** 单纯使用红绿对比（约8%男性为红绿色盲）
   - **推荐：** 蓝橙对比、紫绿对比、蓝黄对比
   - **验证工具：** 使用 Coblis 色盲模拟器检查图表
   ```python
   # 色盲友好配色方案
   sns.set_palette("colorblind")  # Seaborn 色盲友好配色
   ```

**禁止使用的配色：**
- ❌ 深色系（深蓝 `#08519C`、深红 `#A50F15`、深绿 `#006D2C`）
- ❌ 高饱和度纯色（纯红 `#FF0000`、纯蓝 `#0000FF`、纯绿 `#00FF00`）
- ❌ 彩虹色（Jet配色）：在学术界被认为不专业
- ❌ 荧光色（荧光绿、荧光粉）：过于刺眼
- ❌ 黑色背景或深色背景：不适合打印和投影
- ❌ 过多颜色（超过8种）：造成视觉混乱

**配色代码示例：**
```python
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np

# 设置全局配色方案
plt.rcParams['axes.prop_cycle'] = plt.cycler(color=[
    '#6BAED6',  # 浅蓝
    '#FD8D3C',  # 浅橙
    '#74C476',  # 浅绿
    '#9E9AC8',  # 浅紫
    '#FC8D62',  # 浅红
    '#A6D854',  # 浅黄绿
    '#FFD92F',  # 浅黄
    '#E5C494'   # 浅棕
])

# 设置背景为纯白色
plt.rcParams['figure.facecolor'] = 'white'
plt.rcParams['axes.facecolor'] = 'white'

# 示例：对比图
fig, ax = plt.subplots(figsize=(10, 6))
ax.plot(x, y_actual, label='实际值', color='#6BAED6', linewidth=2)
ax.plot(x, y_pred, label='预测值', color='#FD8D3C', linewidth=2, linestyle='--')
ax.legend()
plt.savefig('图1-1_预测对比.png', dpi=300, facecolor='white', bbox_inches='tight')
```

**字体规范：**
- **标题字号：** 14-16pt（加粗）
- **轴标签字号：** 12-14pt
- **刻度字号：** 10-11pt
- **图例字号：** 10-12pt
- **中文字体：** 优先使用 SimHei（黑体）或 Microsoft YaHei（微软雅黑）
- **英文字体：** Times New Roman 或 Arial
  ```

**分辨率要求：**
- **位图格式（PNG/JPG）：** 至少 300 DPI
- **保存代码：**
  ```python
  plt.savefig('图3-1_预测结果对比.png', dpi=300, bbox_inches='tight')


**分辨率要求：**
- **位图格式（PNG/JPG）：** 至少 300 DPI

**图表尺寸：**
- **单图：** 推荐 `figsize=(10, 6)` 或 `figsize=(12, 8)`
- **子图（2x2）：** 推荐 `figsize=(12, 10)` 或 `figsize=(14, 12)`
- **宽图（如时间序列）：** 推荐 `figsize=(14, 5)` 或 `figsize=(16, 6)`

#### 6.2.6 图表排版规则（论文撰写时参考）

**位置安排：**
- 图表应在正文首次提及后立即插入（或在本页/下页）
- 避免跨页：尽量让一个图表完整显示在一页内
- 避免在章节标题正下方或页面最后一行插入图表

**引用格式：**
- ✅ 正确："如图 3-1 所示，模型预测精度显著提升"
- ✅ 正确："各模型性能对比见表 4-2"
- ✅ 正确："从图 5-3 可以看出，误差呈正态分布"
- ❌ 错误："如下图所示"（必须明确图号）
- ❌ 错误："见图1"（应加章节号，如"见图3-1"）

**图表数量建议：**
- 全文图表总数：15-25 个（含表格）
- 每章图表：3-6 个
- 图表比例：图:表 ≈ 2:1（图多于表）

**图表文件管理：**
- 所有图表保存在独立文件夹
- 文件命名规范：图X-Y_简短描述.png
- 同时保存高分辨率PNG
- 创建图表清单文件，记录每个图表的编号、标题、数据来源、关键发现

**6.3 结果可复现性要求**
* **随机种子固定：** 对于包含随机性的算法（GA、PSO、神经网络），必须固定随机种子（如 `seed=42`）
* **参数记录：** 完整记录所有超参数设置
* **环境说明：** 说明所需的软件环境（如 Python 3.8, TensorFlow 2.x）

---

**📦 阶段输出清单：**
- [ ] 求解步骤说明（5步，含参数设置和代码要求）
- [ ] **问题X.py** - 完整的核心求解代码（包含数据处理、模型定义、训练/求解、结果保存）
- [ ] **问题X_可视化.py** - 完整的可视化代码（严格遵循6.2规范，每个图表一个函数）
- [ ] **问题X_结果.csv** - 数值结果文件
- [ ] 每张图表的说明文字（数据来源、关键发现、异常说明）
- [ ] 关键参数调优建议
- [ ] 代码文件结构说明
- [ ] 可复现性检查清单（随机种子、参数记录、环境说明）

**✅ 代码文件检查清单：**
- [ ] 文件命名符合规范（问题X.py, 问题X_可视化.py）
- [ ] 求解代码和可视化代码完全分离
- [ ] 每个可视化函数都有 docstring 说明图表类型和核心信息
- [ ] 图表文件命名包含图号和描述（图X-Y_描述.png）
- [ ] 每张图表都输出了说明文字（数据来源、关键发现、异常说明）
- [ ] 设置了中文字体（避免中文乱码）
- [ ] 设置了随机种子（确保可复现）

✅ **模型求解阶段完成** → 进入 **结果分析阶段**

---

### 七、结果分析阶段

**🎯 阶段目标：** 深度解读结果，提供决策建议，形成论文写作素材。


(针对模型求解结果，给出详细分析，作为论文素材)

**7.1 基础分析（数值解读）**

**7.1.1 关键指标总结**
以表格形式呈现核心结果：

| 指标名称       | 数值   | 单位 | 对比基准        | 改善幅度 |
| :------------- | :----- | :--- | :-------------- | :------- |
| (如：RMSE)     | 0.0523 | -    | 基线模型 0.0847 | ↓ 38.3%  |
| (如：总成本)   | 125.6  | 万元 | 初始方案 158.2  | ↓ 20.6%  |
| (如：求解时间) | 12.3   | 秒   | -               | -        |

**7.1.2 可行性验证**
* **约束满足情况：** 逐一检查所有约束条件是否满足（如 产能约束满足：$\sum y_i = 9850 < 10000$）
* **解的优越性：** 对比初始解、随机解和优化解的目标函数值

**7.2 深层分析（机理洞察）**

**7.2.1 模型行为分析**
结合模型特点，解释为何达到该结果，说明模型的工作机制和关键突破点。

**7.2.2 系统洞察（业务层面）**
* **决策建议：** 基于模型结果，提出可操作的建议
* **风险提示：** 指出模型未考虑的因素

**7.3 实际意义与结论**

**7.3.1 量化商业价值**
将模型结果转化为决策建议，使用具体的量化指标展示优化前后的对比。

**7.3.2 适用范围与推广性**
* **适用条件：** 说明该模型适用的场景和数据条件
* **推广建议：** 说明可扩展的领域和需要调整的参数

**7.3.3 核心结论（论文用）**
提供一段完整的、可直接用于论文的核心结论，包含：模型名称、关键指标提升、创新点、实际应用价值。

---

**📦 阶段输出清单：**
- [ ] 关键指标总结表
- [ ] 约束满足验证报告
- [ ] 模型行为分析（结合算法特点）
- [ ] 系统洞察与决策建议
- [ ] 量化商业价值表
- [ ] 核心结论（论文用语）

✅ **结果分析阶段完成** → 进入 **模型检验与改进阶段**

---

### 八、模型检验与改进阶段

**🎯 阶段目标：** 全面检验模型的有效性、鲁棒性和稳定性，识别局限性，提出改进方向。

**8.1 模型有效性与精度检验 (Validity & Accuracy)**

* **8.1.1 基线模型对比 (Baseline Comparison):**
    * **目的：** 证明创新模型（如 CNN-LSTM）的必要性。
    * **要求：** 选择一个最简单、最基础的“基线模型”进行对比。
    * **可视化：** 采用 **分组柱状图**，对比创新模型与基线模型的「精度指标」和「计算耗时」。
    * **分析：** 说明创新模型相比基线模型的提升百分比。

* **8.1.2 交叉验证 (Cross-Validation):**
    * **目的：** 检验模型的泛化能力。
    * **方法：** 必须使用「K 折交叉验证」或「滚动窗口交叉验证」。
    * **结果：** 输出 K 次验证的平均指标及其标准差。
    * **指标检验 (Metrics Check):** 提供完整的评估指标集（如 RMSE/MAPE 或 混淆矩阵/ROC-AUC 曲线）。

* **8.1.3 误差分布分析：**
    * **可视化：** 采用 **直方图+核密度图**，分析误差的分布形态，判断模型是否存在系统性偏差。
    * **分析：** 检验误差是否呈均值在 0 附近的「正态分布」。

**8.2 模型鲁棒性与稳定性检验 (Robustness & Stability)**

* **8.2.1 参数敏感性分析 (Parameter Sensitivity):**
    * **目的：** 识别对模型结果影响最大的“关键参数”。
    * **方法：** 选取 2-3 个核心参数，在其合理范围（如 $\pm 30\%$）内变化，观察目标函数的变化。
    * **可视化：** 绘制 **多系列折线图** 或 **雷达图**，展示参数变化对结果的影响。
    * **分析：** 结论需明确指出模型对哪些参数**敏感**，对哪些参数**不敏感**（即鲁棒性强）。

* **8.2.2 噪声扰动测试 (Noise Perturbation):**
    * **目的：** 检验模型对“脏数据”的抵抗能力。
    * **方法：** 在**测试集**的输入数据上，人为叠加不同水平的高斯噪声（如 5%, 10%, 20%）。
    * **可视化：** 绘制 **分组柱状图**，对比「无噪声」与「不同水平噪声」下的模型精度。
    * **分析：** 评估模型性能下降的幅度，证明模型的鲁棒性。

* **8.2.3 情景分析 (Scenario Analysis):**
    * **目的：** (针对优化类问题) 测试模型在极端或特殊条件下的表现。
    * **方法：** 设计 2-3 个“压力测试”情景（如 需求冲击、成本冲击、约束变化）。
    * **执行与分析：** 将情景数据代入模型重新求解，比较新情景下的「总成本 / 预测结果 / 是否可行」与原结果的差异，评估方案的**弹性**和**脆弱性**。

**8.3 模型局限性与改进方向 (Limitations & Future Work)**

**8.3.1 局限性分析 (Limitations)**
明确指出模型在以下方面的固有弱点：

| 局限性类别     | 具体问题                                    | 影响程度 | 缓解措施                     |
| :------------- | :------------------------------------------ | :------- | :--------------------------- |
| **数据依赖性** | 需要大量历史数据（n>100），小样本下性能下降 | 中等     | 引入迁移学习或贝叶斯先验     |
| **假设简化**   | 假设需求服从正态分布，实际可能存在厚尾分布  | 较低     | 采用非参数方法或鲁棒统计     |
| **计算成本**   | 训练时间较长（12.3秒），不适合实时决策      | 较低     | 使用模型压缩或增量学习       |
| **可解释性**   | LSTM 为黑箱模型，难以解释单个预测的原因     | 中等     | 引入注意力机制或 SHAP 值分析 |
| **外推能力**   | 对训练集之外的极端情况（如疫情）预测能力弱  | 较高     | 建立情景库，定期重训练       |
| **参数敏感性** | 对学习率高度敏感，需精细调参                | 中等     | 使用自适应学习率或贝叶斯优化 |

**8.3.2 改进方向 (Future Work)**
提出具体的、可执行的未来改进方案：

**方向 1：模型融合（Stacking/Blending）**
* **方案：** 将多个基模型（如 ARIMA, LSTM, XGBoost）的预测结果作为输入，训练一个元模型（如线性回归）进行融合
* **预期效果：** 提升 5%-10% 的预测精度，降低单一模型的风险

**方向 2：引入注意力机制 (Attention Mechanism)**
* **方案：** 在 LSTM 基础上添加注意力层，自动识别对预测最重要的历史时间点
* **预期效果：** 提升可解释性，精度提升 3%-7%

**方向 3：使用更专业的求解器**
* **方案：** 对于优化类问题，使用商业求解器（如 Gurobi, CPLEX）替代启发式算法
* **预期效果：** 求解时间缩短 50%-80%，保证全局最优

**方向 4：在线学习与动态更新**
* **方案：** 建立模型定期重训练机制，每周/每月根据新数据更新参数
* **预期效果：** 提升模型对市场变化的适应性，减少预测偏差

**方向 5：引入外部特征**
* **方案：** 纳入宏观经济指标（如 GDP 增长率、CPI）、舆情数据（如社交媒体情感分析）
* **预期效果：** 提升对突发事件的预测能力，R² 提升至 0.96 以上

---

**📦 阶段输出清单：**
- [ ] 基线模型对比表 + 分组柱状图
- [ ] 交叉验证结果 + 箱线图
- [ ] 完整评估指标表（RMSE, MAPE, R², MAE 等）
- [ ] 误差分布分析 + 直方图+核密度图
- [ ] 参数敏感性分析 + 多系列折线图/雷达图
- [ ] 噪声扰动测试 + 分组柱状图
- [ ] 情景分析表（2-3个情景）
- [ ] 残差分析（如适用）+ 残差图/ACF图/Q-Q图
- [ ] 局限性分析表（6个维度）
- [ ] 改进方向清单（5个方向）

✅ **模型检验与改进阶段完成**

---

### 九、常见问题与最佳实践 (FAQ & Best Practices)

**Q1: 如何判断应该使用传统模型还是机器学习模型？**
* **A:** 根据题目特征和数据量判断：
  - 题目有明确数学关系 → 传统数学模型
  - 样本量 < 100 → 传统统计模型
  - 样本量 ≥ 100 且线性/弱非线性 → 传统模型
  - 强非线性/高维 → 考虑机器学习

**Q2: 代码出现Bug怎么办？**
* **A:** 
  1. **检查数据类型：** 确认输入数据格式（array/list/DataFrame）
  2. **检查维度：** 使用 `print(data.shape)` 检查数据维度
  3. **分段调试：** 在关键步骤插入 `print()` 或断点
  4. **查看文档：** 确认函数参数和返回值
  5. **搜索错误信息：** 复制完整错误信息到搜索引擎
  6. **简化问题：** 用小数据集测试

**Q3: 如果题目数据不足怎么办？**
* **A:** 采取以下策略：
  1. **数据补充：** 查找公开数据源（如政府网站、学术数据库）
  2. **合理假设：** 基于行业均值或历史趋势生成数据（需明确说明假设依据）
  3. **小样本方法：** 使用灰色预测 (GM)、贝叶斯方法等适合小样本的模型
  4. **数据增强：** 对于时序数据，使用插值法扩充；对于图像数据，使用旋转/翻转

**Q4: 如何提升模型的创新性（在传统模型基础上）？**
* **A:** 
  1. **改进目标函数：** 从单目标改为多目标，引入权重调整
  2. **改进约束条件：** 从硬约束改为软约束（加入惩罚项）
  3. **改进求解算法：** 传统算法 + 智能优化（如 LP + GA）
  4. **改进评价体系：** 主观权重 + 客观权重（如 AHP + 熵权法）
  5. **分阶段求解：** 将复杂问题分解为多个子问题，分别求解后整合

**Q5: 如何确保代码的可读性和规范性？**
* **A:** 
  1. **变量命名：** 使用有意义的名称（`total_cost` 而非 `tc`）
  2. **函数拆分：** 单个函数不超过50行，单一职责原则
  3. **添加注释：** 每个函数有 docstring，复杂逻辑有行内注释
  4. **统一格式：** 使用代码格式化工具（如 black, autopep8）
  5. **异常处理：** 使用 try-except 捕获可能的错误
  6. **测试验证：** 对关键函数编写单元测试

**Q6: 传统模型真的比机器学习差吗？**
* **A:** **绝对不是！** 在数学建模竞赛中：
  - **AHP** 在多指标评价中仍然是主流方法
  - **线性规划** 在资源分配问题中无可替代
  - **灰色预测** 在小样本预测中优于神经网络
  - **ARIMA** 在时序预测中可解释性远超 LSTM
  - **评委更看重：** 模型的合理性、可解释性、创新思路，而非算法复杂度

**Q7: 如何在论文中体现代码工作量？**
* **A:** 
  1. **在"模型求解"章节：** 说明代码结构、文件数量、总行数
  2. **在附录中：** 放置核心代码片段（选择最关键的函数）
  3. **在正文中：** 插入算法伪代码（用 LaTeX algorithm 环境）
  4. **提供代码仓库：** 附上 GitHub 链接或代码压缩包

**最佳实践清单（代码相关）：**
- [ ] 所有代码都有详细注释（中文）
- [ ] 关键函数有 docstring（参数、返回值、示例）
- [ ] 设置随机种子确保可复现（`np.random.seed(42)`, `torch.manual_seed(42)`）
- [ ] **代码结构清晰（每个问题必须分为 问题X.py 和 问题X_可视化.py 两个文件）**
- [ ] **求解代码和可视化代码完全分离，不要混在一起**
- [ ] 数据路径使用相对路径（如 `./data/train.csv`）而非绝对路径
- [ ] **图表文件命名规范：图X-Y_描述.png（X=问题号，Y=图序号）**
- [ ] **每张图表都输出说明文字（数据来源、关键发现、异常说明）**
- [ ] 重要结果保存为 CSV/Excel 文件（文件名：问题X_结果.csv）
- [ ] 提供 requirements.txt 或 environment.yml（依赖库清单）
- [ ] 提供 README.md（代码运行说明）
- [ ] 代码运行时间 < 30分钟（超时则需说明）
- [ ] **可视化代码中设置中文字体（避免中文乱码）**
- [ ] **每个可视化函数都注明图表类型和核心信息**

**最佳实践清单（建模相关）：**
- [ ] 每个阶段完成后，检查输出清单是否全部完成
- [ ] 所有图表必须有编号、标题和说明文字
- [ ] 所有公式必须标注物理意义
- [ ] 所有假设必须论证合理性
- [ ] 所有结果必须与基线模型对比
- [ ] 所有参数必须说明取值依据
- [ ] 所有局限性必须坦诚说明
- [ ] 所有改进方向必须具体可行
- [ ] 优先选择可解释性强的模型（在精度相差 < 5% 时）
- [ ] 传统模型足够时，不强行使用机器学习

---

**(任务流程结束) C.M.**

**🎉 完整建模框架已就绪！你可以随时输入 `开始分析题目` 启动分析流程。**
**💡 记住：适合的才是最好的，传统模型并不比机器学习差！代码是数学建模的必需品！**